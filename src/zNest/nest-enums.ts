'use strict';
export enum alertStyle {
    TOASTCTRL,
    ALERTCTRL,
}
export enum gender {
    UNKNOWN,
    MALE,
    FEMALE
}
export enum formSubmitType {
    NEW,
    UPDATE
}

export enum statusType {
    INACTIVE,
    ACTIVE,
}
export enum animation {
    Slide_From_Bottom = 1,
    Slide_From_Top = 2
}